{
  "name": "ipfs-hls",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/hls-ipfs-loader",
    "packages/example-svelte",
    "tests/e2e"
  ],
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
